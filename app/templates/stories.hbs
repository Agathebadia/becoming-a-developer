<NavBar />

<html id="page-stories">

    {{#sortable-table data=@model as |table|}}
      <div class="stories">
        <ul class="results">
          {{#each @model as |story|}}
          <li><Story @story={{story}} /></li>
          {{/each}}
        </ul>
      </div>
    {{/sortable-table}}
</html>

<script>
  /**
  function getSelectedValue() {
      const selectedValue = document.getElementById('list').value;
      console.log(selectedValue);
    }
    //getSelectedValue();

    //if getSelectedValue() === "recent"
    function SortByProperty(prop) {
      return function(a,b) {
        if (a[prop] > b[prop])
          return 1;
       else if(a < b)
       return -1;

      return 0;
        }
    };
    const json = JSON.parse(data);
    json.sort(SortByProperty('id'));
    */
    //if getSelectedValue() === "oldest"
    //if getSelectedValue() === "name"
    //if getSelectedValue() === "year asc"
    //if getSelectedValue() === "year desc"
    //if getSelectedValue() === "developer since asc"
</script>
